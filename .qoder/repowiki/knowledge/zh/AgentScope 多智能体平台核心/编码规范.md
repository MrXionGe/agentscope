- 遵循 Conventional Commits 规范，PR 标题需符合 `<type>(<scope>): <description>` 格式且 scope 必须小写。
- 所有非基础依赖（如特定模型 SDK、Redis 等）必须在函数内部进行懒加载（Lazy Import），避免顶层导入导致的启动开销。
- 强制实施 pre-commit 钩子检查，涵盖 Black 格式化、Flake8 静态分析及 MyPy 类型校验。