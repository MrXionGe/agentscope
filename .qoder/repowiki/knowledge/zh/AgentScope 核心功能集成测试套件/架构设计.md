- **基础设施层**：`utils.py` 提供 `MockModel` 和 `MockCredential`，`test_template.py` 定义基于 `IsolatedAsyncioTestCase` 的异步测试基类。
- **核心逻辑层**：`agent_basic_test.py` 与 `hitl_*_test.py` 覆盖 Agent 的流式响应、工具并发/串行执行及人机交互（HITL）确认流程。
- **生态适配层**：`model_*_test.py` 与 `formatter_*_test.py` 针对 OpenAI、Anthropic 等主流厂商进行 API 对接与消息格式转换的精确比对。
- **环境与扩展层**：`workspace_*_test.py` 验证 Docker/E2B 沙箱的文件持久化与技能加载，`mcp_*_test.py` 确保 Model Context Protocol 客户端的连通性与 Schema 完整性。